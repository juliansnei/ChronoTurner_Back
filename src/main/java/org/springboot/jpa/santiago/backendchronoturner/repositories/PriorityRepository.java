package org.springboot.jpa.santiago.backendchronoturner.repositories;

import org.springboot.jpa.santiago.backendchronoturner.entities.Priority;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PriorityRepository extends JpaRepository<Priority, String> {
    //Atributos de PriorityRepository
    //Constructores de PriorityRepository
    //Asignadores de atributos de PriorityRepository (setters)
    //Lectores de atributos de PriorityRepository (getters)
    //Métodos de PriorityRepository
}
