package org.springboot.jpa.santiago.backendchronoturner.repositories;

import org.springboot.jpa.santiago.backendchronoturner.entities.Task;
import org.springframework.data.jpa.repository.JpaRepository;


public interface TaskRepository extends JpaRepository<Task, String> {
    //Atributos de TaskRepository
    //Constructores de TaskRepository
    //Asignadores de atributos de TaskRepository (setters)
    //Lectores de atributos de TaskRepository (getters)
    //Métodos de TaskRepository
}
