package org.springboot.jpa.santiago.backendchronoturner.repositories;

import org.springboot.jpa.santiago.backendchronoturner.entities.Category;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CategoryRepository extends JpaRepository<Category, String> {
    //Atributos de CategoryRepository
    //Constructores de CategoryRepository
    //Asignadores de atributos de CategoryRepository (setters)
    //Lectores de atributos de CategoryRepository (getters)
    //Métodos de CategoryRepository
}
